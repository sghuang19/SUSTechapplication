---
interface Props {
	date: Date;
}

// FIXME: Use a dumb date for now
// const { date } = Astro.props;
const date = new Date()
---

<time datetime={date.toISOString()}>
	{
		date.toLocaleDateString('en-us', {
			year: 'numeric',
			month: 'short',
			day: 'numeric',
		})
	}
</time>
